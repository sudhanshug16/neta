{
	"name": "@intervene/neta",
	"version": "2.2.2",
	"description": "A leader agent that delegates to tiered worker agents behind the coding-agent CLI you already use.",
	"license": "MIT",
	"author": "Sudhanshu Gautam",
	"homepage": "https://github.com/sudhanshug16/neta#readme",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/sudhanshug16/neta.git"
	},
	"bugs": {
		"url": "https://github.com/sudhanshug16/neta/issues"
	},
	"keywords": [
		"agent",
		"acp",
		"mcp",
		"claude-code",
		"codex",
		"opencode",
		"orchestration"
	],
	"type": "module",
	"bin": {
		"neta": "dist/cli.js"
	},
	"files": [
		"dist",
		"README.md",
		"LICENSE",
		"MANIFESTO.md",
		"CHARTER.example.md",
		"docs",
		"plugins"
	],
	"engines": {
		"node": ">=22.19.0"
	},
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "bun build src/cli.ts --target=node --outdir=dist --sourcemap=linked && chmod +x dist/cli.js",
		"clean": "rm -rf dist",
		"typecheck": "tsc --noEmit",
		"check": "biome check --write --error-on-warnings . && bun run typecheck",
		"test": "bun test",
		"benchmark:checkpoint": "bun run scripts/benchmark-checkpoint.ts",
		"prepublishOnly": "bun run clean && bun run check && bun test && python3 -W error plugins/herdr/neta-status/tests/test_monitor.py && bun run build",
		"refresh-pricing": "bun run scripts/refresh-pricing.ts"
	},
	"devDependencies": {
		"@agentclientprotocol/sdk": "1.3.0",
		"@biomejs/biome": "2.3.5",
		"@earendil-works/pi-tui": "0.84.2",
		"@modelcontextprotocol/sdk": "1.30.0",
		"@types/bun": "1.3.14",
		"@types/node": "24.12.4",
		"diff": "9.0.0",
		"typescript": "5.9.3"
	}
}
